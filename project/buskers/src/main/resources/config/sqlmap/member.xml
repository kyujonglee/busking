<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.buskers.repository.mapper.MemberMapper">
	
	<!-- 일반 회원 가입 -->
	<insert id="signupMember" parameterType="member">
		insert into tb_member (
			id, pass, email, sex, nick_name
		) values (
			#{id}, #{pass}, #{email}, #{sex}, #{nickName}
		);
	</insert>
	
	<!-- 로그인 -->
	<select id="loginAction" resultType="member">
		select *
		  from tb_member
		 where id = #{id}
	</select>
	
	<!-- 아이디 중복체크 -->
<!-- 	<select id="checkId" parameterType="member" resultType="Integer"> -->
<!-- 		select count(*) -->
<!-- 		  from tb_member -->
<!-- 		 where id = #{id} -->
<!-- 	</select> -->
	
	<!-- 이메일 중복체크 -->
<!-- 	<select id="checkId" parameterType="member" resultType="Integer"> -->
<!-- 		select count(*) -->
<!-- 		  from tb_member -->
<!-- 		 where email = #{email} -->
<!-- 	</select> -->
	
	<!-- 닉네임 중복체크 -->
<!-- 	<select id="checkId" parameterType="member" resultType="Integer"> -->
<!-- 		select count(*) -->
<!-- 		  from tb_member -->
<!-- 		 where nick_name = #{nickName} -->
<!-- 	</select> -->
	
	
	
	
	
</mapper>