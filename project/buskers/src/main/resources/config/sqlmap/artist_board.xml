<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.buskers.repository.mapper.ArtistBoardMapper">
	<select id="selectArtistShow" resultType="artistShow">
		select *
		  from tb_busker_show
		 order by enroll_date desc
	</select>

	<select id="selectMainArtistShow" parameterType="artistShow" resultType="artistShow">
		select *
		  from tb_busker_show
		 where gu = #{gu}
		 order by enroll_date desc
	</select>
	
	<insert id="insertArtistShow" parameterType="artistShow">
		insert into tb_busker_show
		(enroll_date, title, content, place, lat, lon, 
		gu, doo, weather, weather_icon,temperature, busker_no)
		values
		(#{enrollDate}, #{title}, #{content}, #{place}, #{lat}, #{lon},
		#{gu}, #{doo},#{weather},#{weatherIcon}, #{temperature}, 1)
	</insert>
	
</mapper>

























